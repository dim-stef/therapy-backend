setup:
  addons:
  - plan: heroku-postgresql
    as: DATABASE
build:
  docker:
    web: Dockerfile
run:
  web: python manage.py runserver 0.0.0.0:8000
release:
  image: web
  command:
    - python manage.py migrate